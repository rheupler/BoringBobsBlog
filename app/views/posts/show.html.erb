<h1><%= @post.title %></h1>
<br>
<h4><%= @post.content %></h4>

<% @post.tags.each do |tag| %>
<%= tag.name %> <%= link_to 'Edit', edit_post_tag_path(@post, tag) %> <%= link_to 'Delete', post_tag_path(@post, tag), data: {confirm: "Are you sure?", method: "delete"}%></li>
<% end %>

<br>

<%= link_to 'New Tag', new_post_tag_path(@post) %>

<%= link_to 'Edit', edit_post_path(@post) %>

<%= link_to 'Delete', post_path(@post), data: {confirm: "Are You Sure?", method: "delete"}%>
